
if platform_is_haiku
    subdir('haiku')
elif platform_is_nacl
    subdir('nacl')
elif platform_is_psp
    subdir('psp')
elif platform_is_winrt
    subdir('winrt')
elif platform_is_windows
    subdir('windows')
else
    subdir('dummy')
endif
