# Tokenizer Fixture

Paragraph with **strong**, _emphasis_, `inline_code`, [a link](https://example.test),
and an autolink <https://example.test/path>.

> A block quote with `code`.

- item one
- item two

```lua
local function greet(name)
  return "hello " .. name
end
```

```js
class Widget { render() { return /ok/.test("ok"); } }
```
