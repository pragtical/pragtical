mesh Th = square(10, 10);
fespace Vh(Th, P1);
Vh u, v;

func f = x * y;
problem Poisson(u, v) =
  int2d(Th)(dx(u) * dx(v) + dy(u) * dy(v))
  - int2d(Th)(f * v)
  + on(1, 2, 3, 4, u = 0);

Poisson;
plot(u, wait = true);
